import { ProductCard } from './product-card';

describe('ProductCard', () => {
  it('should create an instance', () => {
    const directive = new ProductCard();
    expect(directive).toBeTruthy();
  });
});
